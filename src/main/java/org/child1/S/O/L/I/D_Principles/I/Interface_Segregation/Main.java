package org.child1.S.O.L.I.D_Principles.I.Interface_Segregation;

public class Main {
    public static void main(String[] args) {
        //here it says not to make the interface too big that we are forcing subclasses
        //to implement it, infact we can segregate them into small interfaces, so that
        //the subclass implementing it caj use the req fields only, and if we want a bigger
        //interface for one class, or we want the subclass to have more or combined
        //implementation of both the classes then we can implement the 2 smaller interfaces
        //together .

    }
}
